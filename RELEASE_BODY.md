# DRS Smart Keyboard — v1.15.0 (versionCode 27)

## الجولة الخامسة عشرة — «المهام المثبتة 10 فقط» + الوحات الذكية الثلاث

### العنوان
جولة تطوير شاملة خامسة عشرة — V 1.15.0 (versionCode 27): سقف المهام المثبتة بعشرة (طلب المستخدم الصريح: «اجعل المهام المثبته 10فقط مع امكانية تغيرها») + لوحة الحركات الذكية، لوحة الرموز الذكية، لوحة الحروف الموسعة (طلب المستخدم: «واضف ايضآ لوحة الحركات والمزيد من الوحات واضف لها الانظمه الذكيه»).

### 1) المهام المثبتة 10 فقط — قابلة للتغيير دائمًا
- سقف صارم `MAX_PINNED_TOOLS = 10` على رأس شريط المهام: تثبيتات المستخدم أولًا بترتيب تثبيتها، ثم تملأ الافتراضية الأماكن المتبقية فقط — `capPinned` نقية وحتمية.
- رفض صادق للتثبيت الحادي عشر: `setToolPinned`/`setToolPinnedEnsureVisible` تُرجع false ويرفض الدرج الطلب برسالة توست «الحد الأقصى للمهام المثبتة هو 10» — لا انزلاقات صامتة.
- العدّاد المرئي في الدرج: «المثبتة: {count} من {10}» إلى جانب قسم المثبتة الآن.
- إمكانية التغيير كما هي: إعادة ترتيب (أعلى/أسفل)، إزالة تثبيت، تثبيت بديل — وإزالة أي تثبيت تفتح فجوة يملؤها التثبيت التالي بالترتيب حتميًا.
- `resolveFor` نفسها تسقّم الرأس المثبت فالشريط والدرج ومعاينة المدير يقرأون المجموعة المقيّدة نفسها.

### 2) لوحة الحركات الذكية (ImeUiMode.DIACRITICS)
- الدمج الذكي للحركات: حركة جديدة فوق حركة **تستبدلها** بدل أن تتراكم — إعادة النقر على الحركة نفسها عديمة الأثر (استبدال لا تكرار)، والاستثناء الصادق: شدة تليها حركة **تُلحق** (تركيب مشروع)، وحركة تليها شدة تستبدل (خانة حركة واحدة).
- التطويل ليس حركة: حركة بعد التطويل تُلحق دائمًا (ـَ مشروع).
- رقاقات التشكيل المزدوج: (َّ ُّ ِّ ّْ) تُرسل محرفين بنقرة واحدة.
- صف «الأكثر استخدامًا» يقفز للأمام بحسب استخدامك الفعلي محليًا فقط.
- زر إزالة التشكيل في الترويسة يستدعي أداة النص الحقيقية TEXT_TOOL_REMOVE_DIACRITICS.
- الوضع الخفي لا يسجل شيئًا إطلاقًا.

### 3) لوحة الرموز الذكية (ImeUiMode.SMART_SYMBOLS)
- صف اقتراحات سياقية يقرأ النص قبل المؤشر: أرقام → (% ° ÷ × $ ﷼)، حروف عربية → (ـ ، ؛ ؟ « »)، = → (≠ ≈ ≤ ≥)، قوس مفتوح → قافله أولًا، نافذة كود → أقواس — سقف ستة مقترحات حتمية.
- فئات كاملة: رياضيات (19)، عملات (10 شاملة ر.س ود.إ)، أسهم (8)، أقواس (8)، علامات وترقيم (21).
- صف الأكثر استخدامًا + إعادة حساب الاقتراحات بعد كل إدخال من اللوحة نفسها.

### 4) لوحة الحروف الموسعة (ImeUiMode.ARABIC_LETTERS)
- همزات ومشتقات (ء أ إ آ ٱ ؤ ئ ة ى) وحروف الفارسية/الأردية/الكردية (پ چ ژ گ ڤ ک ی ۋ ڭ ۀ ھ).
- صف الأكثر استخدامًا + مبدّل الوحات الذكي (حركات | رموز | حروف) داخل كل لوحة.

### 5) التوصيل الكامل
- ثلاثة رموز مفاتيح جديدة (-218/-219/-220) + ثلاثة أنماط واجهة جديدة داخل قناع الحالة الحالي بلا هجرة بتات.
- أدوات الكتالوج الثلاث بعقد الإلحاق في الذيل → **قابلة للتثبيت ضمن المهام العشر** من الدرج.
- InternalKeys + أيقونات التقييم + أسماء عرض وتلميحات الإجراءات السريعة + عدّادات الاستخدام (SmartToolCodes).
- ثلاثة إعدادات جديدة في التطبيق (شاشة الشريط الذكي → «اللوحات الذكية»): الدمج الذكي للحركات، اقتراحات الرموز، الأكثر استخدامًا — كلها مفاتيح سلوك حقيقية تقرأها اللوحات حيًا.
- سلاسل AR/EN بـ48 مفتاحًا جديدًا (توازي 2310 لكل لغة).

### 6) الاختبارات
- **373 اختبار وحدة ناجحًا** (كانت 341): 32 اختبارًا جديدًا تغطي: capPinned بأربع حالاته (ترتيب المستخدم أولًا، سقف الثلاثين، إزالة التكرار، ملء الافتراضيات)، canPinMore، تسقيم رأس resolveFor بسقوط التثبيت الحادي عشر، الدمج الذكي بتسع حالاته (إلحاق/استبدال/شدة/تطويل/تعطيل)، تصنيف الحركات والتركيبات، الاقتراحات السياقية بسبع حالات، متتبع الأكثر استخدامًا (نمو/ترتيب/تعادل/إزاحة/سقف)، وذكر الأنماط الجديدة.

### التنزيل
- APK: `DRS-Smart-Keyboard-v1.15.0.apk` من صفحة الإصدار.
- الترقية موضعية آمنة من أي إصدار سابق (التوقيع نفسه).
