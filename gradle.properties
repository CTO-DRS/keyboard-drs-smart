android.nonFinalResIds=true
android.nonTransitiveRClass=true
android.useAndroidX=true
kotlin.code.style=official
org.gradle.jvmargs=-Xmx4096m
org.gradle.warning.mode=all

### DRS Keyboard-specific flags ###
projectMinSdk=26
projectTargetSdk=36
projectCompileSdk=36

# === V 1.0.2 — إكمال وتحسين المميزات ===
# V 1.0.2 — preview pill round-2 hardening (explicit editing state,
# uncancellable focus bootstrap, last-resort fallback to the pill, tap haptic)
# + Update Center: upgrade-target chip + how-to-update guide card
projectVersionCode=5
projectVersionName=1.0.2
